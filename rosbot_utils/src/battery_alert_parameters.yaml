---
battery_alert:
  audio_device:
    type: string
    default_value: ''
    description: ALSA device for the alert sound (e.g. plughw:CARD=Device,DEV=0). Empty means auto-detect
      the USB sound card from /proc/asound.

  interval_sec:
    type: int
    default_value: 300
    description: Minimum interval between consecutive alerts in seconds
    validation:
      gt<>: [0]

  percentage_threshold:
    type: double
    default_value: 0.12
    description: 'Battery percentage threshold below which alert is triggered (range: 0.0 - 1.0)'
    validation:
      bounds<>: [0.0, 1.0]
