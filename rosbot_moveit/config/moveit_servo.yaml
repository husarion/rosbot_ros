---
## Properties of incoming commands
command_in_type: speed_units   # "unitless"> in the range [-1:1], as if from joystick. "speed_units"> cmds are in m/s and rad/s
scale:
  # Scale parameters are only used if command_in_type=="unitless"
  linear: 0.3   # Max linear velocity. Unit is [m/s]. Only used for Cartesian commands.
  rotational: 0.8  # Max angular velocity. Unit is [rad/s]. Only used for Cartesian commands.
  # Max joint angular/linear velocity. Only used for joint commands on joint_command_in_topic.
  joint: 0.8

# Optionally override Servo's internal velocity scaling when near singularity or collision (0.0 = use internal velocity scaling)
# override_velocity_scaling_factor = 0.0 # valid range [0.0:1.0]

## Properties of outgoing commands
publish_period: 0.033  # 1/Nominal publish rate [seconds]
low_latency_mode: false  # Set this to true to publish as soon as an incoming Twist command is received (publish_period is ignored)

# What type of topic does your robot driver expect?
# Currently supported are std_msgs/Float64MultiArray or trajectory_msgs/JointTrajectory
command_out_type: trajectory_msgs/JointTrajectory

# What to publish? Can save some bandwidth as most robots only require positions or velocities
publish_joint_positions: true
publish_joint_velocities: true
publish_joint_accelerations: false

## Plugins for smoothing outgoing commands
smoothing_filter_plugin_name: online_signal_smoothing::ButterworthFilterPlugin

# If is_primary_planning_scene_monitor is set to true, the Servo server's PlanningScene advertises the /get_planning_scene service,
# which other nodes can use as a source for information about the planning environment.
# NOTE: If a different node in your system is responsible for the "primary" planning scene instance (e.g. the MoveGroup node),
# then is_primary_planning_scene_monitor needs to be set to false.
is_primary_planning_scene_monitor: true

# Default is "/planning_scene" (absolute) — the leading slash strips the node's namespace, so the
# servo_node_private_* sub-node ends up publishing on /planning_scene. Use a relative name so it
# stays inside the robot namespace.
monitored_planning_scene_topic: planning_scene

## MoveIt properties
move_group_name: manipulator  # Often 'manipulator' or 'arm'
# planning_frame is intentionally the arm's base (link1), not the robot base.
# Cartesian/twist commands and IK are computed relative to the arm root, so the
# manipulator behaves consistently regardless of robot pose / namespace.
planning_frame: link1

## Other frames
ee_frame_name: end_effector_link  # The name of the end effector link, used to return the EE pose
robot_link_command_frame: end_effector_link  # commands must be given in the frame of a robot link. Usually either the base or end effector

## Stopping behaviour
incoming_command_timeout: 0.15   # Stop servoing if X seconds elapse without a new command. Must be > joy_node autorepeat period (50 ms at 20 Hz) - otherwise servo halts between joy messages and the arm stutters. Stop-on-release responsiveness comes from joy2servo publishing a zero-velocity JointJog on stick centring, not from this timeout.
# If 0, republish commands forever even if the robot is stationary. Otherwise, specify num. to publish.
# Important because ROS may drop some messages and we need the robot to halt reliably.
num_outgoing_halt_msgs_to_publish: 4

## Configure handling of singularities and joint limits
# Upstream defaults. The guard runs only on POSE/TWIST commands; joy2servo
# publishes JointJog (which skips it), so the values are inert here. Kept at
# the upstream defaults rather than inflated for clarity — see CLAUDE.md
# 2026-05-15 for why POSE/TWIST is unusable on a 4-DoF arm in jazzy.
lower_singularity_threshold: 17.0
hard_stop_singularity_threshold: 30.0
joint_limit_margin: 0.1 # added as a buffer to joint limits [radians]. If moving quickly, make this larger.
leaving_singularity_threshold_multiplier: 1.0 # Multiply the hard stop limit by this when leaving singularity (see https://github.com/ros-planning/moveit2/pull/620)

## Topic names
cartesian_command_in_topic: ~/delta_twist_cmds  # Topic for incoming Cartesian twist commands
joint_command_in_topic: ~/delta_joint_cmds # Topic for incoming joint angle commands
joint_topic: joint_states
status_topic: ~/status # Publish status to this topic
command_out_topic: manipulator_controller/joint_trajectory # Publish outgoing commands here

## Collision checking for the entire robot body
check_collisions: true # Check collisions?
# 30 Hz instead of the upstream-default 10 Hz: at the joy2servo Cartesian
# clamp (cartesian_max_joint_velocity = 1.0 rad/s) the worst-case tip speed
# is ~0.3 m/s (1 rad/s × ~0.3 m link), so one 100 ms tick covers 30 mm —
# more than the 20 mm proximity threshold, leaving no ramp window. 33 ms
# tick @ 30 Hz drops that to 10 mm.
collision_check_rate: 30.0 # [Hz] Collision-checking can easily bog down a CPU if done too often.
# Kept at the upstream default. 0.05 m halted both modes on startup because
# some non-SRDF-disabled link pairs rest within 5 cm of each other and
# `collision_velocity_scale_` clamped to ~0. The deceleration runway at this
# threshold is short — close approaches at the 1 rad/s clamp will still
# contact — but increasing it without retuning the SRDF bag-of-`Never`s
# trades guaranteed motion for false positives at rest.
self_collision_proximity_threshold: 0.02 # Start decelerating when a self-collision is this far [m]
scene_collision_proximity_threshold: 0.03 # Start decelerating when a scene collision is this far [m]
